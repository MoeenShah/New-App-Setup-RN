import React, {useEffect, useState} from 'react';
import {
  PermissionsAndroid,
  Alert,
  Text,
  View,
  Platform,
  Linking,
  BackHandler,
} from 'react-native';

const Splash = ({}) => {
  return (
    <View>
      <Text>splash</Text>
    </View>
  );
};

export default Splash;
